<UserControl x:Class="Plith.Views.OsdContent"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Plith.ViewModels"
             xmlns:views="clr-namespace:Plith.Views"
             d:DataContext="{d:DesignInstance Type=vm:OsdViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Width="440"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             UseLayoutRounding="True">
    <!-- Outer margin reserves space for the drop-shadow so it doesn't get clipped at window bounds -->
    <Grid Margin="14">
        <Border CornerRadius="14"
                Padding="20,16"
                Background="{StaticResource OsdSurfaceBrush}"
                BorderBrush="{StaticResource OsdBorder}"
                BorderThickness="1"
                SnapsToDevicePixels="True">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="0" BlurRadius="28" Opacity="0.55" Color="Black" />
            </Border.Effect>
            <StackPanel Orientation="Vertical">

                <!-- Media row. The Visibility binding lives on the wrapper Border so it can
                     resolve against OsdViewModel; the inner MediaCard overrides DataContext to
                     Media for its own bindings. -->
                <Border Visibility="{Binding ShowMediaCard, Converter={StaticResource BoolToVisibilityConverter}}">
                    <views:MediaCard x:Name="MediaCardControl"
                                     DataContext="{Binding Media}" />
                </Border>

                <!-- Divider between media + volume; collapses with the card -->
                <Border Height="1"
                        Margin="0,14,0,14"
                        Background="{StaticResource OsdDivider}"
                        Visibility="{Binding ShowMediaCard, Converter={StaticResource BoolToVisibilityConverter}}" />

                <!-- Volume row -->
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="10" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <DockPanel Grid.Row="0" LastChildFill="True">
                        <TextBlock DockPanel.Dock="Right"
                                   Style="{StaticResource OsdValueStyle}"
                                   Text="{Binding GainText}" />
                        <TextBlock Style="{StaticResource OsdLabelStyle}"
                                   VerticalAlignment="Center"
                                   TextTrimming="CharacterEllipsis"
                                   Text="{Binding Label}" />
                    </DockPanel>

                    <Grid Grid.Row="2" Height="6">
                        <Border CornerRadius="3" Background="{StaticResource OsdTrackBg}" />
                        <Border HorizontalAlignment="Left"
                                CornerRadius="3"
                                Background="{Binding GainColor}">
                            <Border.Width>
                                <MultiBinding>
                                    <MultiBinding.Converter>
                                        <vm:NormalizedToWidthConverter />
                                    </MultiBinding.Converter>
                                    <Binding Path="GainNormalized" />
                                    <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                                </MultiBinding>
                            </Border.Width>
                        </Border>
                    </Grid>
                </Grid>

            </StackPanel>
        </Border>
    </Grid>
</UserControl>
