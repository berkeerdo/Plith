<UserControl x:Class="Plith.Views.MediaCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Plith.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:MediaViewModel}"
             mc:Ignorable="d">
    <UserControl.Resources>
        <Style x:Key="MediaButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="36" />
            <Setter Property="Height" Value="36" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="{StaticResource TextPrimary}" />
            <Setter Property="FontFamily" Value="Segoe Fluent Icons, Segoe MDL2 Assets" />
            <Setter Property="FontSize" Value="14" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="bd"
                                CornerRadius="18"
                                Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#22FFFFFF" />
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="bd" Property="Background" Value="#33FFFFFF" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Visibility="{Binding HasSession, Converter={StaticResource BoolToVisibilityConverter}}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="12" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <!-- Album art -->
        <Border Grid.Column="0"
                Width="48" Height="48"
                CornerRadius="6"
                Background="#33FFFFFF"
                ClipToBounds="True">
            <Grid>
                <TextBlock HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontFamily="Segoe Fluent Icons, Segoe MDL2 Assets"
                           FontSize="20"
                           Foreground="{StaticResource TextSecondary}"
                           Text="&#xE940;"
                           Visibility="{Binding HasAlbumArt, Converter={StaticResource InverseBoolToVisibilityConverter}}" />
                <Image Source="{Binding AlbumArt}"
                       Stretch="UniformToFill"
                       Visibility="{Binding HasAlbumArt, Converter={StaticResource BoolToVisibilityConverter}}" />
            </Grid>
        </Border>

        <!-- Title + Artist -->
        <StackPanel Grid.Column="2"
                    VerticalAlignment="Center"
                    Orientation="Vertical">
            <TextBlock Style="{StaticResource CardValueStyle}"
                       FontSize="14"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding Title}" />
            <TextBlock Style="{StaticResource CardLabelStyle}"
                       FontSize="12"
                       Margin="0,2,0,0"
                       TextTrimming="CharacterEllipsis"
                       Text="{Binding Artist}" />
        </StackPanel>

        <!-- Transport controls -->
        <StackPanel Grid.Column="3"
                    Orientation="Horizontal"
                    VerticalAlignment="Center">
            <Button x:Name="PrevButton" Style="{StaticResource MediaButtonStyle}" Content="&#xE892;" ToolTip="Previous" />
            <Button x:Name="PlayPauseButton" Style="{StaticResource MediaButtonStyle}" Content="{Binding PlayPauseGlyph}" ToolTip="Play / Pause" />
            <Button x:Name="NextButton" Style="{StaticResource MediaButtonStyle}" Content="&#xE893;" ToolTip="Next" />
        </StackPanel>
    </Grid>
</UserControl>
