<UserControl x:Class="Plith.Views.BrightnessCardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Plith.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:BrightnessCardViewModel}"
             mc:Ignorable="d"
             AutomationProperties.Name="{Binding AccessibleSummary}"
             AutomationProperties.LiveSetting="Polite">
    <!-- The two AutomationProperties above belong on this UserControl root and nowhere else:
         WPF gives a Grid no automation peer, so a name set there never reaches UI Automation.
         See LiveRegionAnnouncer for the measurement behind that. -->
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="pack://application:,,,/Plith;component/Resources/PlithIcons.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Built from the Audio card's parts rather than from a ProgressBar, which is what the
         plan said. The two cards sit in one stack and a bar with different corners, a
         different height and no glow reads as a second product. -->
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="10" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" LastChildFill="True">
            <Viewbox DockPanel.Dock="Left" Width="21" Height="21" Margin="0,0,10,0"
                     VerticalAlignment="Center">
                <Canvas Width="24" Height="24">
                    <Path Data="{StaticResource IconBrightness}"
                          Stroke="{DynamicResource OsdTextPrimary}"
                          StrokeThickness="1.6" StrokeLineJoin="Round"
                          StrokeStartLineCap="Round" StrokeEndLineCap="Round" />
                </Canvas>
            </Viewbox>

            <TextBlock DockPanel.Dock="Right"
                       Style="{StaticResource OsdValueStyle}"
                       VerticalAlignment="Center"
                       Text="{Binding DisplayText}" />

            <TextBlock FontFamily="{StaticResource OsdUiFont}"
                       FontSize="13"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource OsdTextSecondary}"
                       TextTrimming="CharacterEllipsis"
                       Text="Brightness" />
        </DockPanel>

        <Grid Grid.Row="2" Height="6">
            <Border CornerRadius="3" Background="{DynamicResource OsdTrackBg}" />
            <Border HorizontalAlignment="Left"
                    CornerRadius="3"
                    Background="{DynamicResource OsdAccent}">
                <Border.Width>
                    <MultiBinding>
                        <MultiBinding.Converter>
                            <vm:NormalizedToWidthConverter />
                        </MultiBinding.Converter>
                        <Binding Path="Normalized" />
                        <Binding Path="ActualWidth" RelativeSource="{RelativeSource AncestorType=Grid}" />
                    </MultiBinding>
                </Border.Width>
            </Border>
        </Grid>
    </Grid>
</UserControl>
